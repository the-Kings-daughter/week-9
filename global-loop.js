// Build a simple loop, that print numbers to the console. from 0 to the lenght of an array.

const family = ["Purity", "Peace", "Faith", "Charity", "Splendour", 23];
let size = family.length;
for (let i=0; i <= size; i++) {
    console.log(i)
}